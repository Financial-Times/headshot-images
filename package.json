{
  "name": "@financial-times/headshot-images",
  "engines": {
    "node": "^10"
  },
  "devDependencies": {
    "@financial-times/origami-image-set-tools": "^2.0.0"
  },
  "scripts": {
    "update": "git submodule init && git submodule update --remote && oist build-manifest --scheme fthead && git add imageset.json src && git commit -m 'Updated imageset.json' --allow-empty"
  }
}
